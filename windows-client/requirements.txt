# Audio capture
PyAudio==0.2.14

# Keyboard hotkey detection
keyboard==0.13.5

# HTTP requests to API
requests==2.31.0

# Clipboard access on Windows
pyperclip==1.8.2

# Audio file handling (wave is part of Python standard library - no install needed)
# Configuration loading (json is also standard library)
